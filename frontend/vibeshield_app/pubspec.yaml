name: vibeshield_app
description: "VibeShield AI - Crypto Portfolio Guardian"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.4.9
  
  # UI
  cupertino_icons: ^1.0.2
  fl_chart: ^0.69.0
  google_fonts: ^6.2.1
  
  # Network
  dio: ^5.7.0
  
  # Web3
  web3dart: ^2.7.3
  reown_appkit: ^1.0.0
  url_launcher: ^6.3.1
  shared_preferences: ^2.3.3
  
  # Utils
  intl: ^0.19.0
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
